/**
 * Card & card-required API used by admin (re-export from utils/api).
 */
export { cardApi, getCardImageUrl, cardRequiredApi } from '@/utils/api';
export type { Card, CardRequired } from '@/utils/api';
